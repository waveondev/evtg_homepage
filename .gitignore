# 1. 의존성 패키지 (매우 무거우므로 제외)
node_modules/

# 2. Next.js 빌드 결과물
.next/
out/
build/

# 3. 환경 변수 파일 (보안 중요! 절대 올리면 안 됨)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 4. 운영체제 및 에디터 설정
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# 5. 로그 및 캐시
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.eslintcache
.stylelintcache

# 6. 정적 리소스 관련 (필요 시 수정)
# 만약 public 폴더 안에 영상이나 큰 파일이 계속 쌓인다면 아래처럼 제외 가능
# public/videos/*.mp4
